package ClientServerSocket;
import java.net.*;
import java.io.*;

public class NewClientThread extends Thread {
    //here we see the first explicit example of inheritance, as our class "NewClientThread" extends, and therefore
    //inherits the methods, objects and attributes from its superclass Thread.

}
